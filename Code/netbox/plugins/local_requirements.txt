# NetBox Plugin Requirements
# These are installed automatically when the netbox container starts.
# Uncomment the plugins you want to activate.

# ── Lifecycle (EoL / EoS tracking) ──────────────────────────────────────────
# Tracks EOL/EOS dates at DeviceType level (model-wide hardware lifecycle).
# Also tracks Support Contracts (start/end, Active/Future/Expired) per device.
# Replaces custom fields: service_start_date, service_end_date, service_tags, eos
# Docs: https://github.com/DanSheps/netbox-lifecycle
netbox-lifecycle

# ── Inventory (physical asset tracking) ─────────────────────────────────────
# Adds asset tags, ownership and storage location tracking.
# Docs: https://github.com/ArnesSI/netbox-inventory
# netbox-inventory
